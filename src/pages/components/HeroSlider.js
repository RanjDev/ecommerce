import React from "react";

export default function HeroSlider({ product }) {
  return (
    <div>
      WE MIGHT NEED TO GET ALL THE PRODUCTS TOGETHER
      <p>Hero SLider</p>
      <p>{product.name}</p>+ a link to the products page
    </div>
  );
}
